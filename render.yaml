services:
  - type: web
    name: ai-pcra-frontend-backend
    env: docker
    plan: free
    region: oregon
    branch: main
    buildCommand: ""
    startCommand: uvicorn server:app --host 0.0.0.0 --port 8000
    dockerfilePath: Backend/dockerfile
    autoDeploy: true
